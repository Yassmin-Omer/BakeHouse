pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build'
                sh '''
                echo ${BUILD_NUMBER}
                '''
            }
        }
    }


}


